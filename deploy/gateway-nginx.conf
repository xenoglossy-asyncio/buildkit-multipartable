# Registry Gateway — consistent-hash routes to multiple registry backends.
# Each backend has its own outbound IP for Docker Hub pulls.
# All backends share the same S3 bucket for cache.

upstream registry_backends {
    hash $request_uri consistent;
    server registry-0:80;
    server registry-1:80;
    server registry-2:80;
}

server {
    listen 80;

    # Max upload size for image pushes
    client_max_body_size 2g;

    # Proxy to registry backends
    location / {
        proxy_pass http://registry_backends;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # Registry-specific: disable buffering for blob upload/download
        proxy_buffering off;
        proxy_request_buffering off;

        # Long timeouts for large layer pushes
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }
}
