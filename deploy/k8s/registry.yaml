# Registry backends + gateway.
# All registry instances share the same S3 bucket.
# Each has hostNetwork for unique outbound IP (Docker Hub egress).
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: registry-backend
spec:
  serviceName: registry-backend
  replicas: 3
  selector:
    matchLabels:
      app: registry-backend
  template:
    metadata:
      labels:
        app: registry-backend
    spec:
      hostNetwork: true           # unique outbound IP per pod
      dnsPolicy: ClusterFirstWithHostNet
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: registry-backend
              topologyKey: kubernetes.io/hostname
      containers:
        - name: registry
          image: registry:2
          ports:
            - containerPort: 5000
          env:
            - name: REGISTRY_STORAGE_S3_ACCESSKEY
              valueFrom:
                secretKeyRef:
                  name: s3-credentials
                  key: access-key
            - name: REGISTRY_STORAGE_S3_SECRETKEY
              valueFrom:
                secretKeyRef:
                  name: s3-credentials
                  key: secret-key
            - name: REGISTRY_STORAGE_S3_REGION
              value: us-east-1
            - name: REGISTRY_STORAGE_S3_REGIONENDPOINT
              value: http://minio:9000
            - name: REGISTRY_STORAGE_S3_BUCKET
              value: dtbuildkit-registry
            - name: REGISTRY_STORAGE_S3_SECURE
              value: "false"
            - name: REGISTRY_STORAGE_S3_V4AUTH
              value: "true"
            - name: REGISTRY_STORAGE_S3_CHUNKSIZE
              value: "5242880"
            - name: REGISTRY_STORAGE_S3_ROOTDIRECTORY
              value: /
            - name: REGISTRY_STORAGE_DELETE_ENABLED
              value: "true"
---
apiVersion: v1
kind: Service
metadata:
  name: registry-backend
spec:
  clusterIP: None  # headless — each pod gets DNS record
  selector:
    app: registry-backend
  ports:
    - port: 5000
---
# Registry Gateway (consistent-hash reverse proxy)
apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-gateway-config
data:
  nginx.conf: |
    upstream registry_backends {
        hash $request_uri consistent;
        server registry-backend-0.registry-backend:5000;
        server registry-backend-1.registry-backend:5000;
        server registry-backend-2.registry-backend:5000;
    }
    server {
        listen 5000;
        client_max_body_size 2g;
        location / {
            proxy_pass http://registry_backends;
            proxy_set_header Host $host;
            proxy_buffering off;
            proxy_request_buffering off;
            proxy_read_timeout 600s;
            proxy_send_timeout 600s;
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: registry-gateway
  template:
    metadata:
      labels:
        app: registry-gateway
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: config
              mountPath: /etc/nginx/conf.d
      volumes:
        - name: config
          configMap:
            name: registry-gateway-config
---
apiVersion: v1
kind: Service
metadata:
  name: registry-gateway
spec:
  selector:
    app: registry-gateway
  ports:
    - port: 5000
