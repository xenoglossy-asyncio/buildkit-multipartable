apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dtbuild-server-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dtbuild-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dtbuild-server
  template:
    metadata:
      labels:
        app: dtbuild-server
    spec:
      containers:
        - name: server
          image: dtbuildkit:latest
          command: ["/app/dtbuild-server"]
          args:
            - "--addr=:8000"
            - "--db=/data/dtbuildkit.db"
            - "--blobs=/data/blobs"
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: dtbuild-server-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: dtbuild-server
spec:
  selector:
    app: dtbuild-server
  ports:
    - port: 8000
      targetPort: 8000
---
# Registry mirror as a pull-through cache for Docker Hub base images
apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-mirror-config
data:
  config.yml: |
    version: 0.1
    storage:
      cache:
        blobdescriptor: inmemory
      filesystem:
        rootdirectory: /var/lib/registry
    http:
      addr: :5001
    proxy:
      remoteurl: https://registry-1.docker.io
      ttl: 168h
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry-mirror
spec:
  replicas: 1
  selector:
    matchLabels:
      app: registry-mirror
  template:
    metadata:
      labels:
        app: registry-mirror
    spec:
      containers:
        - name: registry
          image: registry:2
          ports:
            - containerPort: 5001
          volumeMounts:
            - name: config
              mountPath: /etc/docker/registry
            - name: data
              mountPath: /var/lib/registry
      volumes:
        - name: config
          configMap:
            name: registry-mirror-config
        - name: data
          persistentVolumeClaim:
            claimName: registry-mirror-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: registry-mirror
spec:
  selector:
    app: registry-mirror
  ports:
    - port: 5001
      targetPort: 5001
---
# Shared registry for image output + layer cache
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: registry
  template:
    metadata:
      labels:
        app: registry
    spec:
      containers:
        - name: registry
          image: registry:2
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: data
              mountPath: /var/lib/registry
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: registry-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: registry
spec:
  selector:
    app: registry
  ports:
    - port: 5000
      targetPort: 5000
